## 1. 输入拦截

- [ ] 1.1 `handleKeyDown` 增加死亡状态检查，死亡时忽略所有键盘输入
- [ ] 1.2 `handleClick` 增加死亡状态检查，死亡时不请求指针锁定
- [ ] 1.3 `handleMouseDown` 增加死亡状态检查，死亡时不触发射击或倍镜切换

## 2. 指针与光标控制

- [ ] 2.1 `watch(isDead)` 中增加 `document.exitPointerLock()`，死亡时释放指针锁定
- [ ] 2.2 死亡时通过 inline style 将光标设为 `pointer`，复活时恢复

## 3. 验证

- [ ] 3.1 确认死亡界面按钮可正常点击
- [ ] 3.2 确认死亡后无法开枪
- [ ] 3.3 确认死亡后键盘操作无效
- [ ] 3.4 确认复活后恢复正常游戏操作和光标
